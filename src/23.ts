/** Apply */
export type Apply = unknown;

/** Push an element to a tuple */
export type Push = unknown;

/** Filter a tuple */
export type Filter = unknown;

/** Determine if the given type extends another */
export type Extends = unknown;

/** Apply an operation to all inputs */
export type ApplyAll = unknown;

/** Capitalize a string */
export type Cap = unknown;
